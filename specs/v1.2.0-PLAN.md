# Gemini MCP v1.2.0 开发任务清单

> 基于 [docs/PRD.md](../docs/PRD.md) 生成
> 创建时间: 2026-01-27

---

## 任务概览

| # | 任务 | 预计时间 | 状态 |
|---|------|---------|------|
| 1 | 更新配置常量 | 15min | [x] |
| 2 | 删除废弃工具文件 | 10min | [x] |
| 3 | 更新工具定义 | 30min | [x] |
| 4 | 更新工具导出索引 | 10min | [x] |
| 5 | 更新服务器路由 | 15min | [x] |
| 6 | 修改 multimodal_query 工具 | 20min | [x] |
| 7 | 修改 analyze_content 工具 | 20min | [x] |
| 8 | 修改 analyze_codebase 工具 | 20min | [x] |
| 9 | 修改 brainstorm 工具 | 20min | [x] |
| 10 | 修改 search 工具 | 20min | [x] |
| 11 | 构建并测试 | 30min | [x] |
| 12 | 更新版本号和文档 | 15min | [x] |

---

## 验收检查点

- [x] **检查点 A**（Task 1-5 完成后）：基础架构修改验收
- [x] **检查点 B**（Task 6-10 完成后）：工具功能验收
- [x] **检查点 C**（Task 11-12 完成后）：最终集成验收

---

## Task 1: 更新配置常量

**目标**: 修改 `src/config/constants.ts`，删除已移除工具的常量

### AI 提示词

```
你是一位资深 TypeScript 开发专家，精通 Node.js 和 MCP 协议。

请修改文件 `src/config/constants.ts`：

1. 在 TOOL_NAMES 对象中，删除以下常量：
   - LIST_MODELS
   - GENERATE_UI
   - FIX_UI
   - CREATE_ANIMATION（如果存在）

2. 保留以下常量：
   - MULTIMODAL_QUERY
   - ANALYZE_CONTENT
   - ANALYZE_CODEBASE
   - BRAINSTORM
   - SEARCH

3. 确保删除后代码无语法错误

代码注释请使用中文。
```

### 验收标准
- [ ] TOOL_NAMES 只包含 5 个工具常量
- [ ] 代码无语法错误
- [ ] TypeScript 编译通过

---

## Task 2: 删除废弃工具文件

**目标**: 删除不再使用的工具文件

### AI 提示词

```
你是一位资深 TypeScript 开发专家。

请删除以下文件：
- src/tools/list-models.ts
- src/tools/generate-ui.ts
- src/tools/fix-ui.ts

使用 Bash 工具执行删除命令。
```

### 验收标准
- [ ] 三个文件已删除
- [ ] git status 显示文件被删除

---

## Task 3: 更新工具定义

**目标**: 修改 `src/tools/definitions.ts`，删除废弃工具定义，为保留工具添加 model 参数

### AI 提示词

```
你是一位资深 TypeScript 开发专家，精通 MCP 协议的工具定义 JSON Schema。

请修改文件 `src/tools/definitions.ts`：

1. 删除以下工具的定义：
   - gemini_generate_ui
   - gemini_fix_ui_from_screenshot
   - list_models

2. 为以下 5 个工具的 inputSchema.properties 添加 model 参数：
   - gemini_multimodal_query
   - gemini_analyze_content
   - gemini_analyze_codebase
   - gemini_brainstorm
   - gemini_search

3. model 参数定义如下：
   ```typescript
   model: {
     type: 'string',
     enum: ['gemini-3.1-pro-preview', 'gemini-3-flash-preview'],
     description: '使用的 Gemini 模型 (可选，默认: gemini-3.1-pro-preview)'
   }
   ```

4. 确保 TOOL_DEFINITIONS 数组只包含 5 个工具

代码注释请使用中文。
```

### 验收标准
- [ ] TOOL_DEFINITIONS 只包含 5 个工具
- [ ] 每个工具都有 model 参数定义
- [ ] TypeScript 编译通过

---

## Task 4: 更新工具导出索引

**目标**: 修改 `src/tools/index.ts`，删除废弃工具的导出

### AI 提示词

```
你是一位资深 TypeScript 开发专家。

请修改文件 `src/tools/index.ts`：

1. 删除以下导出：
   - handleListModels (from './list-models.js')
   - handleGenerateUI (from './generate-ui.js')
   - handleFixUI (from './fix-ui.js')

2. 保留以下导出：
   - handleMultimodalQuery
   - handleAnalyzeContent
   - handleAnalyzeCodebase
   - handleBrainstorm
   - handleSearch

代码注释请使用中文。
```

### 验收标准
- [ ] 只导出 5 个工具处理函数
- [ ] 无导入错误
- [ ] TypeScript 编译通过

---

## Task 5: 更新服务器路由

**目标**: 修改 `src/server.ts`，删除废弃工具的路由

### AI 提示词

```
你是一位资深 TypeScript 开发专家，精通 Node.js 服务器开发。

请修改文件 `src/server.ts`：

1. 在 import 语句中，删除对以下函数的导入：
   - handleListModels
   - handleGenerateUI
   - handleFixUI

2. 在 handleToolsCall 函数的 switch 语句中，删除以下 case：
   - TOOL_NAMES.LIST_MODELS
   - TOOL_NAMES.GENERATE_UI
   - TOOL_NAMES.FIX_UI

3. 保留以下 case：
   - TOOL_NAMES.MULTIMODAL_QUERY
   - TOOL_NAMES.ANALYZE_CONTENT
   - TOOL_NAMES.ANALYZE_CODEBASE
   - TOOL_NAMES.BRAINSTORM
   - TOOL_NAMES.SEARCH

代码注释请使用中文。
```

### 验收标准
- [ ] 只有 5 个工具的路由
- [ ] 无导入错误
- [ ] TypeScript 编译通过

---

## 🔷 检查点 A：基础架构修改验收

完成 Task 1-5 后暂停，执行以下验收：

```bash
# 1. 检查 TypeScript 编译
npm run build

# 2. 检查删除的文件
ls src/tools/

# 3. 检查常量定义
grep -n "TOOL_NAMES" src/config/constants.ts
```

**验收通过后说"继续"进入下一阶段**

---

## Task 6: 修改 multimodal_query 工具

**目标**: 修改 `src/tools/multimodal-query.ts`，支持 model 参数

### AI 提示词

```
你是一位资深 TypeScript 开发专家，精通 Gemini API。

请修改文件 `src/tools/multimodal-query.ts`：

1. 在 MultimodalQueryParams 接口中添加可选的 model 参数：
   ```typescript
   model?: 'gemini-3.1-pro-preview' | 'gemini-3-flash-preview';
   ```

2. 在 handleMultimodalQuery 函数中：
   - 如果 params.model 存在，调用 client.setModel(params.model)
   - 默认使用 'gemini-3.1-pro-preview'

3. 在返回结果中包含 modelUsed 字段

代码注释请使用中文。
```

### 验收标准
- [ ] 支持 model 参数
- [ ] 默认使用 gemini-3.1-pro-preview
- [ ] 返回结果包含 modelUsed

---

## Task 7: 修改 analyze_content 工具

**目标**: 修改 `src/tools/analyze-content.ts`，支持 model 参数

### AI 提示词

```
你是一位资深 TypeScript 开发专家，精通 Gemini API。

请修改文件 `src/tools/analyze-content.ts`：

1. 在 AnalyzeContentParams 接口中添加可选的 model 参数：
   ```typescript
   model?: 'gemini-3.1-pro-preview' | 'gemini-3-flash-preview';
   ```

2. 在 handleAnalyzeContent 函数中：
   - 如果 params.model 存在，调用 client.setModel(params.model)
   - 默认使用 'gemini-3.1-pro-preview'

3. 在返回结果中包含 modelUsed 字段

代码注释请使用中文。
```

### 验收标准
- [ ] 支持 model 参数
- [ ] 默认使用 gemini-3.1-pro-preview
- [ ] 返回结果包含 modelUsed

---

## Task 8: 修改 analyze_codebase 工具

**目标**: 修改 `src/tools/analyze-codebase.ts`，支持 model 参数

### AI 提示词

```
你是一位资深 TypeScript 开发专家，精通 Gemini API。

请修改文件 `src/tools/analyze-codebase.ts`：

1. 在 AnalyzeCodebaseParams 接口中添加可选的 model 参数：
   ```typescript
   model?: 'gemini-3.1-pro-preview' | 'gemini-3-flash-preview';
   ```

2. 在 handleAnalyzeCodebase 函数中：
   - 删除硬编码的 model: 'gemini-3.1-pro-preview'
   - 使用 params.model || 'gemini-3.1-pro-preview'

3. 在返回结果中包含 modelUsed 字段

代码注释请使用中文。
```

### 验收标准
- [ ] 支持 model 参数
- [ ] 默认使用 gemini-3.1-pro-preview
- [ ] 返回结果包含 modelUsed

---

## Task 9: 修改 brainstorm 工具

**目标**: 修改 `src/tools/brainstorm.ts`，支持 model 参数

### AI 提示词

```
你是一位资深 TypeScript 开发专家，精通 Gemini API。

请修改文件 `src/tools/brainstorm.ts`：

1. 在 BrainstormParams 接口中添加可选的 model 参数：
   ```typescript
   model?: 'gemini-3.1-pro-preview' | 'gemini-3-flash-preview';
   ```

2. 在 handleBrainstorm 函数中：
   - 如果 params.model 存在，调用 client.setModel(params.model)
   - 默认使用 'gemini-3.1-pro-preview'

3. 在返回结果中包含 modelUsed 字段

代码注释请使用中文。
```

### 验收标准
- [ ] 支持 model 参数
- [ ] 默认使用 gemini-3.1-pro-preview
- [ ] 返回结果包含 modelUsed

---

## Task 10: 修改 search 工具

**目标**: 修改 `src/tools/search.ts`，支持 model 参数

### AI 提示词

```
你是一位资深 TypeScript 开发专家，精通 Gemini API。

请修改文件 `src/tools/search.ts`：

1. 在 SearchParams 接口中添加可选的 model 参数：
   ```typescript
   model?: 'gemini-3.1-pro-preview' | 'gemini-3-flash-preview';
   ```

2. 在 handleSearch 函数中：
   - 删除硬编码的 const model = 'gemini-3.1-pro-preview'
   - 使用 const model = params.model || 'gemini-3.1-pro-preview'

3. 在返回结果中的 modelUsed 字段使用实际模型

代码注释请使用中文。
```

### 验收标准
- [ ] 支持 model 参数
- [ ] 默认使用 gemini-3.1-pro-preview
- [ ] 返回结果包含正确的 modelUsed

---

## 🔷 检查点 B：工具功能验收

完成 Task 6-10 后暂停，执行以下验收：

```bash
# 1. 检查 TypeScript 编译
npm run build

# 2. 检查所有工具文件都有 model 参数
grep -l "model\?: " src/tools/*.ts
```

**验收通过后说"继续"进入下一阶段**

---

## Task 11: 构建并测试

**目标**: 构建项目并进行功能测试

### AI 提示词

```
你是一位资深 TypeScript 开发专家，精通 Node.js 测试。

请执行以下操作：

1. 运行 TypeScript 编译：
   npm run build

2. 如果编译失败，分析错误并修复

3. 编译成功后，检查 dist/ 目录生成的文件

4. 确保没有引用已删除文件的错误

代码注释请使用中文。
```

### 验收标准
- [ ] npm run build 成功
- [ ] dist/ 目录包含编译后的 JS 文件
- [ ] 无 TypeScript 错误

---

## Task 12: 更新版本号和文档

**目标**: 更新 package.json 版本号，更新 README 文档

### AI 提示词

```
你是一位资深开发专家，精通项目发布流程。

请执行以下操作：

1. 修改 package.json：
   - 将 version 从 "1.1.1" 改为 "1.2.0"

2. 修改 README.md：
   - 更新可用工具列表（只保留 5 个工具）
   - 添加 model 参数的说明
   - 删除已移除工具的文档

3. 创建 CHANGELOG.md 条目（如果存在该文件）：
   - 添加 v1.2.0 的变更记录

代码注释请使用中文。
```

### 验收标准
- [ ] package.json version 为 "1.2.0"
- [ ] README.md 已更新
- [ ] 文档与代码一致

---

## 🔷 检查点 C：最终集成验收

完成 Task 11-12 后，执行最终验收：

```bash
# 1. 检查版本号
grep "version" package.json

# 2. 最终构建
npm run build

# 3. 检查工具数量
grep -c "name:" src/tools/definitions.ts
```

**全部验收通过后，可以提交代码并发布新版本**

---

## 完成状态

- 总任务数: 12
- 已完成: 0
- 进行中: 0
- 待开始: 12
